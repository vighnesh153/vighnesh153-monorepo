/**
 * Creates an external package in the packages/ directory
 */
export async function createExternalPackage(directoryName: string, packageName: string) {
  throw new Error('External package creation is not built');
  console.log(directoryName, packageName);
}
