/**
 * Creates an internal package in the packages/ directory
 */
export async function createInternalPackage(directoryName: string, packageName: string) {
  console.log(directoryName, packageName);
}
