---
import type { HTMLAttributes } from 'astro/types';

import BaseTypography, { type TypographyVariant } from './base-typography.astro';

export interface Props extends HTMLAttributes<'h5'> {
  variant?: TypographyVariant;
}

const { variant = 'h5', ...props } = Astro.props;
---

<BaseTypography element="h5" variant={variant} htmlProps={props}>
  <slot />
</BaseTypography>
