---
import type { HTMLAttributes } from 'astro/types';

import BaseTypography, { type TypographyVariant } from './base-typography.astro';

export interface Props extends HTMLAttributes<'h2'> {
  variant?: TypographyVariant;
}

const { variant = 'h2', ...props } = Astro.props;
---

<BaseTypography element="h2" variant={variant} htmlProps={props}>
  <slot />
</BaseTypography>
